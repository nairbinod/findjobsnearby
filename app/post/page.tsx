"use client";
/* eslint-disable @next/next/no-html-link-for-pages */

import { FormEvent, useState } from "react";

export default function PostPage() {
  const [draft, setDraft] = useState(false);
  const [published, setPublished] = useState(false);
  const [title, setTitle] = useState("");
  const [pay, setPay] = useState("");
  const [location, setLocation] = useState("");
  const [type, setType] = useState("Full-time");
  const [responsibilities, setResponsibilities] = useState("");

  function createDraft(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setDraft(true);
    setPublished(false);
  }

  return (
    <div className="min-h-screen bg-[var(--cream)]">
      <header className="mx-auto flex max-w-[1000px] items-center justify-between px-6 py-6 lg:px-10"><a href="/" className="display text-[25px] font-bold tracking-[-.04em]">findjobs<span className="text-[var(--coral)]">nearby</span><sup className="ml-0.5 text-[10px]">®</sup></a><a href="/jobs" className="text-sm font-bold text-[var(--muted)]">Browse jobs <span aria-hidden="true">→</span></a></header>
      <main className="mx-auto max-w-[1000px] px-6 pb-20 pt-12 lg:px-10"><div className="mb-12 max-w-[650px]"><p className="mb-4 text-xs font-bold uppercase tracking-[.2em] text-[var(--coral)]">Free employer posting</p><h1 className="display text-5xl font-bold leading-[.95] tracking-[-.04em] sm:text-7xl">Tell us who<br />you&apos;re looking for.</h1><p className="mt-6 text-lg leading-7 text-[var(--muted)]">A few details in. A clear job listing out. You&apos;ll review everything before it goes live.</p></div><div className="grid gap-8 lg:grid-cols-[1fr_.85fr]"><form onSubmit={createDraft} className="rounded-2xl bg-white p-6 shadow-sm sm:p-8"><div className="mb-8 flex items-center gap-3 text-sm font-bold"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[var(--ink)] text-white">1</span><span>Role details</span><span className="h-px flex-1 bg-[var(--line)]" /><span className="text-[var(--muted)]">Free to post</span></div><div className="space-y-5"><label className="block text-sm font-bold">Job title<input required value={title} onChange={(event) => setTitle(event.target.value)} placeholder="e.g. Front desk coordinator" className="mt-2 w-full rounded-xl border border-[var(--line)] px-4 py-3 font-normal outline-none transition-colors focus:border-[var(--coral)]" /></label><div className="grid gap-5 sm:grid-cols-2"><label className="block text-sm font-bold">Pay range <span className="text-[var(--coral)]">*</span><input required value={pay} onChange={(event) => setPay(event.target.value)} placeholder="e.g. $18-22/hr" className="mt-2 w-full rounded-xl border border-[var(--line)] px-4 py-3 font-normal outline-none focus:border-[var(--coral)]" /></label><label className="block text-sm font-bold">Employment type<select value={type} onChange={(event) => setType(event.target.value)} className="mt-2 w-full rounded-xl border border-[var(--line)] bg-white px-4 py-3 font-normal outline-none"><option>Full-time</option><option>Part-time</option><option>Contract</option><option>Seasonal</option></select></label></div><label className="block text-sm font-bold">City or neighborhood<input required value={location} onChange={(event) => setLocation(event.target.value)} placeholder="e.g. Bishop Arts, Dallas" className="mt-2 w-full rounded-xl border border-[var(--line)] px-4 py-3 font-normal outline-none focus:border-[var(--coral)]" /></label><label className="block text-sm font-bold">What will they do?<span className="mt-1 block text-xs font-normal text-[var(--muted)]">Add 3-5 responsibilities. One per line.</span><textarea required value={responsibilities} onChange={(event) => setResponsibilities(event.target.value)} rows={5} placeholder="Prepare daily orders\nHelp customers at the counter\nKeep the workspace organized" className="mt-2 w-full resize-none rounded-xl border border-[var(--line)] px-4 py-3 font-normal outline-none focus:border-[var(--coral)]" /></label></div><button type="submit" className="mt-8 w-full rounded-full bg-[var(--coral)] px-6 py-4 font-bold text-white shadow-[0_6px_0_#ce5a4b] transition-transform hover:translate-y-0.5">Draft my listing <span aria-hidden="true">→</span></button></form><aside className="rounded-2xl border border-[var(--line)] bg-[var(--mint)] p-6 sm:p-8"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.15em] text-[var(--coral)]">{published ? "Published" : draft ? "Review before publishing" : "Your listing preview"}</p><span className="text-xl">✳</span></div>{published ? <div className="mt-16 text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-[var(--yellow)] text-2xl">✓</div><h2 className="display mt-6 text-3xl font-bold">You&apos;re live.</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Your listing is ready for local job seekers to discover.</p></div> : draft ? <div className="mt-10"><h2 className="display text-4xl font-bold">{title}</h2><p className="mt-2 font-semibold">{location} · {type}</p><p className="mt-1 font-bold text-[var(--coral)]">{pay}</p><div className="mt-8 border-t border-[var(--ink)]/15 pt-5"><p className="text-sm leading-7">{responsibilities.split("\n").filter(Boolean).map((item) => <span className="block" key={item}>• {item}</span>)}</p></div><p className="mt-8 text-xs leading-5 text-[var(--muted)]">AI-assisted draft. Only the details you provided are included. Nothing publishes until you approve it.</p><button onClick={() => setPublished(true)} className="mt-6 w-full rounded-full bg-[var(--ink)] px-6 py-4 font-bold text-white">Approve &amp; publish <span aria-hidden="true">↗</span></button></div> : <div className="mt-12"><div className="h-4 w-24 rounded bg-white/70" /><div className="mt-5 h-10 w-4/5 rounded bg-white/70" /><div className="mt-3 h-4 w-2/5 rounded bg-white/70" /><div className="mt-10 space-y-3 border-t border-[var(--ink)]/10 pt-6"><div className="h-3 w-full rounded bg-white/60" /><div className="h-3 w-11/12 rounded bg-white/60" /><div className="h-3 w-4/5 rounded bg-white/60" /></div><p className="mt-12 text-sm leading-6 text-[var(--muted)]">Your approved listing will be clear, grounded in your words, and ready to share with nearby candidates.</p></div>}</aside></div></main>
    </div>
  );
}
